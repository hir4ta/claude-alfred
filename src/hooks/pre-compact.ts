import type { HookEvent } from "../types.ts";

/** PreCompact: save structured handoff state */
export default async function preCompact(_ev: HookEvent): Promise<void> {
	// TODO: Phase 3 — structured handoff
}
