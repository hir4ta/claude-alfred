import type { HookEvent } from "../types.ts";

/** Stop: review enforcement + pace warning */
export default async function stop(_ev: HookEvent): Promise<void> {
	// TODO: Phase 3 — review enforcement + pace
}
