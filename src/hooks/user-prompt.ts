import type { HookEvent } from "../types.ts";

/** UserPromptSubmit: Plan template injection */
export default async function userPrompt(_ev: HookEvent): Promise<void> {
	// TODO: Phase 2 — Plan template injection
}
