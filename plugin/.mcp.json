{
  "mcpServers": {
    "alfred": {
      "args": [
        "serve"
      ],
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/run.sh",
      "env": {
        "VOYAGE_API_KEY": "${VOYAGE_API_KEY}"
      }
    }
  }
}
