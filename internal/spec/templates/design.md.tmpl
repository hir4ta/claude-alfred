# Design: {{.TaskSlug}}

## Architecture Overview
<!-- confidence: _ | source: code -->
<!-- required -->
<!-- Describe the architecture from three perspectives:
     1. **Layers**: What are the major layers/packages? (e.g., handler → service → store)
     2. **Data Flow**: How does data move through the system for the primary use case?
     3. **Boundaries**: Where are the trust/API/module boundaries? What crosses them?
     If helpful, include an ASCII diagram:
     ```
     [input] → [handler] → [service] → [store] → [DB]
     ```
-->
{Describe layers, data flow, and boundaries — see guidance above}

## Design Tenets
<!-- required -->
<!-- Ordered by priority. Used to break ties when design trade-offs arise.
     Format: {N}. {Tenet Name} over {Alternative}
     Example: 1. Simplicity over Performance — prefer readable code unless profiling proves a bottleneck -->
1. {Tenet} over {Alternative}
2. {Tenet} over {Alternative}

## System Context
<!-- required -->
<!-- C4 Level 1: how does this feature fit in the whole system?
     Show external actors, other systems, and this feature's boundary -->
{External users/systems} → [This Feature] → {Downstream dependencies}

## Container Diagram
<!-- required for M/L/XL — skip for S-sized tasks -->
<!-- C4 Level 2: processes, data stores, and external systems that compose the solution.
     Show runtime containers (not code modules):
     - Process: a running executable (CLI, daemon, lambda, etc.)
     - Data Store: database, file system, cache, queue
     - External System: third-party API, upstream service
     Include communication protocols between containers.
     Example:
     ```
     [CLI Process] --gRPC--> [API Server] --SQL--> [PostgreSQL]
                                          --HTTP--> [Voyage AI API]
     [Hook Process] --file I/O--> [SQLite DB]
     ```
-->
{Containers, data stores, and external systems — see guidance above}

## Deployment Topology
<!-- optional: skip for S-sized tasks -->
<!-- Where does this run? What are the runtime constraints?
     Include: OS, architecture, single-user vs multi-user, local vs remote, resource limits -->
- **Runtime**: {e.g., macOS local CLI, single-user}
- **Storage**: {e.g., SQLite file at ~/.alfred/alfred.db}
- **External Dependencies**: {e.g., Voyage AI API (optional)}

## Component Design
<!-- required -->
<!-- Component granularity guidance:
     Create a NEW component when:
     - It has a distinct responsibility (single-responsibility principle)
     - It could be tested independently
     - It has its own failure modes
     EXTEND an existing component when:
     - The new behavior is a natural extension of its current responsibility
     - Splitting would create circular dependencies
     - The "component" would be a single function with no state
-->

### Component: {name}
- **File**: `path/to/file.go`
- **Responsibility**: {what it does}
- **Interface**:
  ```
  {interface or function signatures}
  ```
- **Dependencies**: {other components}
- **NFRs**: {NFR-N that this component must satisfy, or "—"}

<!-- example:
### Component: JWTValidator
- **File**: `pkg/auth/jwt.go`
- **Responsibility**: JWT token validation and refresh
- **Interface**:
  ```go
  type JWTValidator interface {
      ValidateToken(token string) (*Claims, error)
      RefreshToken(claims *Claims) (string, error)
  }
  ```
- **Dependencies**: `pkg/store`, `pkg/config`
- **NFRs**: NFR-1 (respond within 200ms), NFR-2 (no plaintext secrets in logs)
-->

## Sequence Diagram
<!-- optional: skip for S-sized tasks -->
<!-- Use Mermaid syntax for interaction flows. The ASCII diagram above shows static architecture;
     sequence diagrams show runtime behavior.
     Example (indented, not fenced):
       sequenceDiagram
         participant C as Client
         participant H as Handler
         participant S as Store
         C->>H: Request
         H->>S: Query
         S-->>H: Result
         H-->>C: Response
-->

## Data Models
<!-- optional: skip for S-sized tasks -->

### Entity: {Name}
```sql
CREATE TABLE {table_name} (
    id   TEXT PRIMARY KEY,
    -- {fields}
);
```

## API Contracts
<!-- optional: skip for S-sized tasks -->

### {METHOD} {/api/endpoint}
- **Request**: `{ "field": "type" }`
- **Response 200**: `{ "field": "type" }`
- **Response 4xx**: `{ "error": "code" }`
- **Maps to**: FR-{N}

## Requirements Traceability
<!-- required -->

| Req ID | Component | Interface | Task ID | Test ID |
|--------|-----------|-----------|---------|---------|
| FR-1   | {component} | {interface} | T-1.1 | TS-1.1 |
| NFR-1  | {component} | {interface} | T-{N.N} | TS-{N.N} |

## Error Handling Design
<!-- required — for S-sized tasks, a single sentence describing the error strategy suffices -->
<!-- Define error categories and recovery strategies for this feature.
     Include timeout values and retry policies for any I/O or network operations.
     Retry policy format: {strategy} ({attempts}x, {backoff type}, {max wait})
     Example: Exponential backoff (3x, 100/200/400ms, max 700ms) -->

| Error Category | Example | Recovery Strategy | Timeout | Retry Policy | User Impact |
|---------------|---------|-------------------|---------|-------------|-------------|
| {category} | {specific error} | {retry/fallback/fail-fast/degrade} | {timeout or N/A} | {retry policy or N/A} | {what user sees} |

## Observability Design
<!-- optional: skip for S-sized tasks -->
| Signal | Name/Pattern | When | FR |
|--------|-------------|------|-----|
| Log | {log.Info/Warn/Error pattern} | {trigger} | FR-{N} |
| Metric | {metric_name} | {what it measures} | NFR-{N} |

## Security Design
<!-- optional: skip for S-sized tasks -->
| Threat | Asset | Mitigation | Trust Boundary |
|--------|-------|------------|----------------|
| {threat} | {asset} | {mitigation} | {boundary} |

## Migration Strategy
<!-- optional: skip for S-sized tasks -->
1. {Step 1 — backward compatible}
2. {Step 2}

## Tech Decisions (Quick Reference)
- **{topic}**: {chosen} → decisions.md #DEC-{N}
