# Bugfix: {{.TaskSlug}}

<!-- ## Confidence Calibration Guide (reference for all spec files)
     9-10: Verified — tested, benchmarked, or explicitly confirmed by user
      7-8: Reviewed — design-reviewed or derived from authoritative docs
      5-6: Inferred — reasoned from code analysis or domain knowledge
      3-4: Hypothetical — educated guess, needs validation
      1-2: Speculative — placeholder, likely to change
     Source types: user (direct instruction), design-doc, code (analysis), inference, assumption
     When confidence ≤ 5 AND source = assumption, dossier status will flag a warning.
-->

## Bug Summary
<!-- confidence: _ | source: user -->
{{.Description}}

## Severity & Impact
<!-- required — classify the bug severity and set SLA expectations -->
<!-- Severity guide:
     P0-Critical: System down, data loss, security breach. SLA: 4 hours
     P1-High: Major feature broken, no workaround. SLA: 1 business day
     P2-Medium: Feature degraded, workaround exists. SLA: 1 week
     P3-Low: Cosmetic, minor inconvenience. SLA: backlog
-->

| Severity | SLA Target | Impact Scope |
|----------|-----------|--------------|
| {P0/P1/P2/P3} | {4h/1d/1w/backlog} | {affected users/features/data} |

- **User impact**: {who is affected and how}
- **Workaround**: {temporary workaround if any, or "None"}

## Reproduction Steps
<!-- List the exact steps to reproduce the bug. Include environment details if relevant. -->
1. Step 1
2. Step 2
3. Observe unexpected behavior

- **Environment**: {OS, Go version, relevant config}
- **Frequency**: {always / intermittent / specific conditions}

## Current Behavior
<!-- What actually happens (the bug). Be specific about error messages, incorrect output, etc. -->

## Expected Behavior
<!-- What should happen instead. Reference existing docs/specs if available. -->

## Unchanged Behavior
<!-- EARS: "SHALL CONTINUE TO" — behaviors that MUST NOT be affected by this fix.
     Use this to prevent regressions and scope creep. -->
- The system SHALL CONTINUE TO {existing behavior that must be preserved}

## Root Cause Analysis
<!-- Identify the root cause. Reference specific files, functions, or logic paths. -->
<!-- confidence: _ | source: code -->
<!-- RCA methodology: use 5 Whys to drill down to the true root cause.
     Why 1: {symptom} → because {direct cause}
     Why 2: {direct cause} → because {underlying cause}
     Why 3: {underlying cause} → because {deeper cause}
     Why 4: {deeper cause} → because {systemic issue}
     Why 5: {systemic issue} → because {root cause}
     Stop when you reach a cause you can fix. Not all bugs need 5 levels. -->

### 5 Whys Analysis
1. **Why** does {symptom occur}? → Because {cause 1}
2. **Why** does {cause 1}? → Because {cause 2}
3. **Why** does {cause 2}? → Because {root cause}

**Root Cause**: {final root cause — the actionable fix target}

## Fix Strategy
<!-- Describe the approach to fix the bug. Keep it minimal and surgical. -->

## Regression Prevention
<!-- required — specify how to prevent this bug from recurring -->
- **Test case**: {describe the test that will catch this bug if reintroduced}
- **Test file**: `{path/to/test_file.go}`
- **Test type**: {Unit / Integration / E2E}
- **What it verifies**: {specific assertion that would have caught this bug}

## Assumptions & Constraints
- Fix should be minimal and surgical — avoid unrelated refactoring
- Existing tests must continue to pass
