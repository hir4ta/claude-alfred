version: 2

builds:
  - binary: claude-buddy
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

brews:
  - repository:
      owner: hir4ta
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/hir4ta/claude-buddy
    description: "Proactive session companion for Claude Code — anti-pattern detection, destructive command blocking, and usage coaching"
    license: MIT
    install: |
      bin.install "claude-buddy"
    caveats: |
      Run 'claude-buddy install' to register hooks and MCP server.

      AI feedback language is auto-detected from your system locale (LANG).
      To override: LANG=ja_JP.UTF-8 claude-buddy
    test: |
      system "#{bin}/claude-buddy", "help"

changelog:
  sort: asc
